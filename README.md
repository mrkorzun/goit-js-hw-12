# Пошук зображень через Pixabay API

![JavaScript](https://img.shields.io/badge/JavaScript-F7DF1E?style=flat-square&logo=javascript&logoColor=black)
![Vite](https://img.shields.io/badge/Vite-646CFF?style=flat-square&logo=vite&logoColor=white)
![Axios](https://img.shields.io/badge/Axios-5A29E4?style=flat-square&logo=axios&logoColor=white)
![HTML5](https://img.shields.io/badge/HTML5-E34F26?style=flat-square&logo=html5&logoColor=white)
![CSS3](https://img.shields.io/badge/CSS3-1572B6?style=flat-square&logo=css3&logoColor=white)
![Prettier](https://img.shields.io/badge/Prettier-F7B93E?style=flat-square&logo=prettier&logoColor=black)

Домашнє завдання №11 курсу **JavaScript** від **GoIT**. <br>Створення застосунку
для пошуку зображень за ключовим словом з використанням публічного API сервісу
**Pixabay**, бібліотеки **Axios** для HTTP-запитів та модального вікна
**SimpleLightbox** для перегляду повнорозмірних зображень.

<p align="center">
  <img src="./preview.jpg" alt="Preview" width="900">
</p>

---

## Зміст

- [Про проєкт](#про-проєкт)
- [Технології](#технології)
- [Структура проєкту](#структура-проєкту)
- [Опис модулів](#опис-модулів)
- [Функціональність](#функціональність)
- [Параметри HTTP-запиту](#параметри-http-запиту)
- [Використані властивості об'єкта зображення](#використані-властивості-обєкта-зображення)
- [Встановлення та запуск](#встановлення-та-запуск)
- [Демо](#демо)
- [Автор](#автор)

---

## Про проєкт

Застосунок дозволяє користувачу вводити ключове слово та переглядати галерею
зображень, отриманих із публічного сервісу Pixabay. Реалізовано модульну
архітектуру з розділенням відповідальності: HTTP-запити, рендеринг інтерфейсу та
основна бізнес-логіка винесені в окремі файли.

Ключові акценти проєкту:

- асинхронна взаємодія з REST API через бібліотеку **Axios**;
- обробка успішних і невдалих відповідей через `.then()` / `.catch()`;
- валідація вводу та сповіщення користувача через **iziToast**;
- галерея з модальним переглядом на базі **SimpleLightbox**;
- візуальний індикатор завантаження з бібліотеки **css-loader**.

---

## Технології

| Технологія                                                   | Призначення                            |
| ------------------------------------------------------------ | -------------------------------------- |
| **[Vite](https://github.com/mrkorzun/vanilla-app-template)** | Збірка проєкту та dev-сервер           |
| **JavaScript (ES6+)**                                        | Основна мова, модульна архітектура     |
| **HTML5 / CSS3**                                             | Розмітка та стилізація                 |
| **[Axios](https://axios-http.com/)**                         | Виконання HTTP-запитів до Pixabay API  |
| **[Pixabay API](https://pixabay.com/api/docs/)**             | Джерело даних — пошук зображень        |
| **[SimpleLightbox](https://simplelightbox.com/)**            | Модальне вікно для перегляду зображень |
| **[iziToast](https://marcelodolza.github.io/iziToast/)**     | Стилізовані повідомлення-сповіщення    |
| **[CSS Loader](https://cssloaders.github.io/)**              | Анімований індикатор завантаження      |
| **Prettier**                                                 | Форматування коду                      |

---

## Структура проєкту

```
goit-js-hw-11/
├── src/
│   ├── js/
│   │   ├── pixabay-api.js
│   │   └── render-functions.js
│   ├── css/
│   │   └── styles.css
│   └── main.js
├── index.html
├── vite.config.js
├── package.json
└── README.md
```

---

## Опис модулів

### `pixabay-api.js`

Модуль, що відповідає за взаємодію з бекендом. Експортує функцію виконання
HTTP-запиту до Pixabay API.

| Функція                   | Параметри       | Повертає                            |
| ------------------------- | --------------- | ----------------------------------- |
| `getImagesByQuery(query)` | `query: string` | Значення `data` з відповіді сервера |

### `render-functions.js`

Модуль, що відповідає за рендеринг інтерфейсу та керування галереєю. Містить
екземпляр `SimpleLightbox` та функції роботи з DOM.

| Функція                 | Параметри       | Опис                                                                                  |
| ----------------------- | --------------- | ------------------------------------------------------------------------------------- |
| `createGallery(images)` | `images: Array` | Створює розмітку карток і додає її в контейнер галереї, викликає `lightbox.refresh()` |
| `clearGallery()`        | —               | Очищає вміст контейнера галереї перед новим пошуком                                   |
| `showLoader()`          | —               | Додає клас, що відображає індикатор завантаження                                      |
| `hideLoader()`          | —               | Видаляє клас, що приховує індикатор завантаження                                      |

### `main.js`

Точка входу застосунку. <br>Імпортує функції з модулів `pixabay-api.js` та
`render-functions.js`, описує всю бізнес-логіку:

- обробку події `submit` на формі пошуку;
- валідацію введеного запиту;
- послідовність викликів `clearGallery()` → `showLoader()` → запит →
  `hideLoader()` → `createGallery()`;
- обробку порожньої відповіді через `iziToast`.

---

## Функціональність

- При сабміті форми виконується HTTP-запит до Pixabay API з пошуковим словом,
  введеним користувачем.
- Перед запитом галерея очищається, відображається індикатор завантаження.
- Після отримання відповіді індикатор зникає, у DOM додається розмітка карток
  **за одну операцію**.
- Якщо результатів немає — виводиться `iziToast`-повідомлення:

  ```
  Sorry, there are no images matching your search query. Please try again!
  ```

- При кліку на картку відкривається повнорозмірне зображення в модальному вікні
  `SimpleLightbox`.
- Усі помилки HTTP-запитів обробляються в `.catch()` та виводяться користувачу
  через `iziToast`.

---

## Параметри HTTP-запиту

Запит до Pixabay API формується з обов'язковим набором параметрів:

| Параметр      | Значення           | Призначення                        |
| ------------- | ------------------ | ---------------------------------- |
| `key`         | особистий API-ключ | Унікальний ключ доступу до сервісу |
| `q`           | рядок із форми     | Пошуковий запит користувача        |
| `image_type`  | `photo`            | Лише фотографії                    |
| `orientation` | `horizontal`       | Горизонтальна орієнтація           |
| `safesearch`  | `true`             | Фільтр безпечного контенту         |

---

## Використані властивості об'єкта зображення

Зі структури об'єкта, що повертає Pixabay, в галереї використовуються:

| Властивість     | Призначення                            |
| --------------- | -------------------------------------- |
| `webformatURL`  | Маленьке зображення для картки галереї |
| `largeImageURL` | Велике зображення для модального вікна |
| `tags`          | Опис — підставляється в атрибут `alt`  |
| `likes`         | Кількість вподобайок                   |
| `views`         | Кількість переглядів                   |
| `comments`      | Кількість коментарів                   |
| `downloads`     | Кількість завантажень                  |

---

## Встановлення та запуск

### Клонування репозиторію

```bash
git clone https://github.com/mrkorzun/goit-js-hw-11
cd goit-js-hw-11
```

### Встановлення залежностей

```bash
npm install
```

### Запуск dev-сервера

```bash
npm run dev
```

### Збірка продакшн-версії

```bash
npm run build
```

### Деплой на GitHub Pages

```bash
npm run deploy
```

---

## Демо

- **Жива сторінка:** [`https://mrkorzun.github.io/goit-js-hw-11/`](#)
- **Вихідний код:** [`https://github.com/mrkorzun/goit-js-hw-11`](#)

### Прев'ю реалізації

![Preview](./preview.png)

---

## Автор

Розроблено в рамках навчального курсу **GoIT — Front-End / Full Stack
JavaScript**.
